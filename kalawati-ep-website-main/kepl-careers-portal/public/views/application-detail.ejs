<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Application Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <div class="container mt-4">
        <h2 class="mb-4">Application Details</h2>
        
        <div class="card">
            <div class="card-header">
                <h4><%= application.fullName %> - <%= application.position %></h4>
            </div>
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <h5>Personal Information</h5>
                        <p><strong>Email:</strong> <%= application.email %></p>
                        <p><strong>Phone:</strong> <%= application.phone %></p>
                        <p><strong>Nationality:</strong> <%= application.nationality %></p>
                        <p><strong>Marital Status:</strong> <%= application.maritalStatus || 'N/A' %></p>
                        <p><strong>Driving License:</strong> <%= application.drivingLicense || 'N/A' %></p>
                    </div>
                    <div class="col-md-6">
                        <h5>Application Details</h5>
                        <p><strong>Date:</strong> <%= application.date.toLocaleDateString() %></p>
                        <p><strong>Employment Type:</strong> <%= application.employmentType %></p>
                        <p><strong>Status:</strong> <span class="badge bg-<%= 
                            application.status === 'Pending' ? 'warning' : 
                            application.status === 'Reviewed' ? 'info' : 
                            application.status === 'Hired' ? 'success' : 'danger' 
                        %>"><%= application.status %></span></p>
                    </div>
                </div>
                
                <div class="row mb-3">
                    <div class="col-12">
                        <h5>Address</h5>
                        <p><%= application.address %></p>
                        <p><%= application.cityState %>, <%= application.zipcode %></p>
                    </div>
                </div>
                
                <% if (application.resumePath) { %>
                    <div class="mt-3">
                        <a href="/admin/application/resume/<%= application._id %>" class="btn btn-primary">Download Resume</a>
                    </div>
                <% } %>
                
                <div class="mt-4">
                    <a href="/admin/applications" class="btn btn-secondary">Back to Applications</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>